#include "main.h"
void _puts(char *str)
#include<stdio.h>
 /**
 *Return: Always 0 (Success)
 *
 * main - Entry point
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
